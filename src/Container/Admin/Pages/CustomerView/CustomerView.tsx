import React from 'react';

const CustomerView = () => {
    return (
        <div className={'page_responsive'}>
            Customer View Page
        </div>
    );
};

export default CustomerView;
